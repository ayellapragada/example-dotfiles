# .zshrc - Zsh shell configuration
# Add your zsh configurations here
# This file is managed by dotfiles repo

# Source bash aliases if they exist
if [[ -f ~/.bash_aliases ]]; then
    source ~/.bash_aliases
fi

